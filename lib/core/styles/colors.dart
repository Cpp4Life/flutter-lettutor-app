import 'package:flutter/material.dart';

class LetTutorColors {
  // Primary
  static const primaryBlue = Color.fromRGBO(0, 113, 240, 1);

  // Secondary
  static const secondaryDarkBlue = Color.fromRGBO(42, 52, 83, 1.0);
}
